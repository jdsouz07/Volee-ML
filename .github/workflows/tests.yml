# Runs the test suite on every push and pull request.
# Tests that need downloaded data or a trained model skip themselves here;
# the Glicko parity, score parsing, leakage, rating and adapter tests all run.
name: tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: pytest -q
